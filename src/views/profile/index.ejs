<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <title>Perfil</title>
</head>
<body>
    <div class="container">
        <h2>Perfil de @<%= posts[0]['user_info']['username'] %></h2>

        <% posts.forEach(post => { %>
            <div class="card mt-2">
                <div class="card-body">
                    <img class="rounded-circle" src="http://localhost:5000/img/photos/<%= post['user_info']['profile'] %>" width="50" height="50">
                    <%= post['user_info']['username'] %>
                </div>
                <img src="http://localhost:5000/img/posts/<%= post['media'] %>" width="100%">
                <div class="card-body">
                    <div class="card-title">
                        <form action="/addLike" method="POST">
                            <input type="hidden" name="post_id" value="<%= post.post_id %>">
                            <input type="hidden" name="origin" value="profile/<%= post['user_info']['user_id'] %>">
                            <button type="submit" class="btn btn-danger"><%= post['likes'].length %></button>
                        </form>
                    </div>
                    <p class="card-text"><%= post['title'] %></p>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>